<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
        margin: 0;
        padding: 0;
        height: 100vh;
        background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
        font-family: 'Montserrat', sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        overflow: hidden;
        color: #333;
        animation: fadeIn 1s ease-in-out;
    }
    @keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

    /* Adding a Circular Gradient Background */
    body::before {
        content: "";
        position: absolute;
        top: 50%;
        left: 50%;
        width: 200vw;
        height: 200vh;
        background: radial-gradient(circle, #ffd3e2 0%, #ff6b6b 100%);
        z-index: -1;
        transform: translate(-50%, -50%);
        opacity: 0.7;
    }
 
       .container{
        text-align: center;
       }
       .header{
         padding: 50px;
         margin-bottom: 50px;
         font-size:500%;
       }
       .tittle{
        color: #333;
        padding-bottom: 10px;
        margin-bottom: 10px;
        font-size: xx-large;
        display: block;
       }
       .aamount{
        color: #333;
        padding-bottom: 10px;
        margin-bottom: 10px;
        font-size: xx-large;
        display: block;
       }
       .ccategory{
        color: #333;
        padding-bottom: 10px;
        margin-bottom: 10px;
        font-size: xx-large;
        display: block;
       }
       .www1{
        height:30px;
        border-radius: 10px;
        background-color: #f3f3f3;
        transition: all 0.3s ease;
        box-shadow: inset 0 2px 5px rgba(0, 0, 0, 0.05);
        border: 1px solid #ccc;
       }
       .www1:focus {
        border-color: #ff6b6b;
        background-color: #fff;
        outline: none;
        box-shadow: 0 0 10px rgba(255, 107, 107, 0.2);
    }
        ::placeholder {
        color: #888;
        font-style: italic;
        font-size: large;
    }
       
    .button {
        font-size: 20px;
        padding: 15px 30px;
        background-color: #ff6b6b;
        color: white;
        border: none;
        border-radius: 30px;
        cursor: pointer;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
        overflow: hidden;
    }

    .button::before {
        content: "";
        position: absolute;
        top: 0;
        left: -100%;
        width: 200%;
        height: 100%;
        background: linear-gradient(to right, #ffe259, #ffa751);
        transition: all 0.3s ease;
        z-index: -1;
    }

    .button:hover::before {
        left: 0;
    }

    .button:hover {
        transform: scale(1.1);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
    }

    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit-Expense</title>
</head>
<body>
    <div class="container">
    <h1 class="header">Edit Expense</h1>
    <form action="/expenses/<%= expense._id %>/edit" method="POST">
     
    <div class="tittle">
    <label for="title">Title: </label>
    <input type="text" name="title" id="title" value="<%= expense.title %>" required class="www1" placeholder="Car">
    </div>
    <br>
    
    <div class="aamount">
    <label for="amount">Amount:</label>
    <input type="number" name="amount" id="amount" value="<%= expense.amount %>" required class="www1" placeholder="2000000">
    </div>
    <br>
    
    <div class="ccategory">
    <label for="category">Category:</label>
    <input type="text" name="category" id="category" value="<%= expense.category %>" required class="www1" placeholder="TATA Safari">
    </div>
    <br>
    
    <button type="submit" class="button">Update Expense</button>
</form>
    </div>
</body>
</html>